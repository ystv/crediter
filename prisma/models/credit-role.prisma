model CreditRole {
  id                       String  @id @default(cuid())
  name  String

  order Int

  names CreditRoleName[]

  event_id String
  event Event @relation(fields: [event_id], references: [id])

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt()

  @@map("credit_role")
}